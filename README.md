This is my implementation of a Red Black Tree. A Data structure that is a spin off from the conventional Binary Search Tree (BST). 

It tackles the problem of a long time complexity of when searching for a node with a particular value as a conventional Binary Search Tree can be 'unbalaced' and leading to a worst case time complexity of O(n) and a Red Black Tree has reduces this to O(log(n))!


